# 인계 문서 — 2026-08-26 (2차 갱신)

## 0. 한 줄 상태

main = origin/main = `bd1dd6ad`.
**이번 라운드는 조사 전용이었다 — 사용자 지시로 코드 수정을 하지 않았다.** 27개 워크플로우
스웜이 199/224 레인을 완주해 발견 1,058건(critical 12 · high 87)을 남겼고, 그중 다섯이
서로 다른 워크플로우에서 독립 도달해 교차 확정됐다. 산출은 전부 `docs/` 에 실려 푸시됐다.

**다음 세션의 본체는 그 다섯 중 ①②를 고치는 것이다.** 파스 절반은 이미 착지해 있다(§3).

## 1. 이번 라운드 커밋 (전부 푸시됨)

| 커밋 | 내용 |
| --- | --- |
| `c9f63fff` | 감사 문서 2편 + `docs/README.md` 색인 — `full-audit-2026-08-26.md`(16레인), `swarm-audit-2026-08-26.md`(27 워크플로우) |
| `70d1c0ec` | 재생정책 6키 파스 배선(`ProjectJSONParser`·`WallpaperProject`) — **소비자 없음, 동작 변화 0** |
| `bd1dd6ad` | `cross-analysis-2026-08-25.html` 을 추적으로 이동(종전 untracked) |

테스트 수 변화 없음(3,708). CI 하한 래칫 불필요.

## 2. 교차 확정 5건 — 다음 라운드 우선순위

상세·좌표는 `docs/swarm-audit-2026-08-26.md` §3. 여기엔 착수 순서만 적는다.

### ① 전역 캡처 핀 `capturePointerUV` (7개 워크플로우 독립 도달)

`a328d339`(직전 라운드 P0) 가 넣은 `nonisolated(unsafe) static var` 가 세 층위에서 깨진다.

- **데이터레이스** — 백그라운드 큐가 쓰고 메인 액터(`startPointerMonitor`·`updateParallax`)가
  잠금 없이 읽는다.
- **라이브 오염** — 캡처 창(무거운 씬은 수 초) 중 새 씬이 마운트되면 그 인스턴스가 캡처
  하네스로 오인돼 모니터 없이 태어난다. 커서 반응이 다음 재마운트까지 영구 사망한다.
  `desktopStillSync` 가 켜져 있으면 적용마다 3초 뒤 캡처가 도니 흔한 경로다.
- **잔여 구멍** — 게이트는 포인터·미디어폴러 둘뿐이고 **클릭 모니터(`SceneRenderer.swift:849`)는
  무게이트**다. 물리 클릭이 `g_PointerState.z` 임펄스로 골든 캡처에 구워진다.
  BACKLOG "잔여 갭 2건" ①이 정확히 이것이고, 직전 라운드 툼스톤이 "해소" 라고 쓴 문단이
  실제로는 3중 1개를 빠뜨렸다.
- **핀 영구 잔존** — 동시 캡처 2건의 `defer` 복원이 교차하면 핀이 안 풀린다.

착수 순서: 클릭 모니터 게이트 한 줄 → 핀을 인스턴스 소유로 바꾸거나 직렬화 → 툼스톤 문단 정정.

### ② 재생정책 사슬 전면 미배선 (5개 워크플로우)

모델·평가기는 **완성됐고 WE 실물과 7계약 전부 일치한다** — P 레인이 `config.json` ×
`scripts.js` 드롭리스트 × `locale` 라벨 × 바이너리 사원 수렴으로 확정했다(`PlaybackPolicy.swift:114`
의 weDefault 가 글자 그대로 맞다). 그런데 소비자가 없다.

- 앱 실행 타깃이 `WaplePolicy` 에 **의존조차 하지 않는다**(`Package.swift:49`) — 첫 단추는
  매니페스트다.
- `playbackProperties`(§3) 소비자 0개.
- 파서 주석이 약속한 "앱 측 감시 테스트" 는 없고, **현 타깃 그래프로는 만들 수도 없다**
  (WapleCore 와 WaplePolicy 를 동시에 보는 testTarget 이 없다).
- `AppLogic` 의 `PresetResolver.resolve` 가 프로젝트를 필드 재나열로 재구성하며 이 필드를
  **드롭한다** — 프리셋 경유 마운트에서만 정책이 소실되므로, 배선이 착지하는 순간 발화한다.
  배선 전에 같이 고쳐야 한다.

### ③ `PlaylistTransition.swift` 849행이 데드 코드

WE 전환효과 27종을 포팅해 놓고 앱 런타임에 연결하지 않았다 — 모든 배경 교체가 hard-cut 이다.
`daytime` 모드·경과시간 영속(`playliststatetime.bin`)·모니터별 재생목록 상태 분리도 부재.

### ④ RE 근거 좌표 +0xD0 시프트 (critical, 짝 저장소)

`inject_rich_header.py` 가 PE 본문을 +0xD0 밀면서 섹션 raw_ptr 을 갱신하지 않아
**디컴파일 코퍼스 11,252 함수의 주소가 전부 어긋났다**. Waple 소스 주석의 핸들러 VA 인용
20여 개가 이 규칙(`실제 VA = 인용값 + 0xD0`)으로 교정 대상이다.

더 큰 문제: `.pdata` 실측 함수는 14,788개인데 코퍼스는 11,252개이고 시작점 교집합이 0.6% —
**오퍼레이터 VM·파서 영역이 통째로 누락**돼 있다. 그 영역을 다시 볼 때는 디컴파일 코퍼스가
아니라 r2/직접 PE 파싱으로 가야 한다(C·G·L 레인이 그렇게 했다).

### ⑤ 수치 진실성 — CI 래치와 정본 셈법이 갈렸다

`ci.yml` 게이트는 3708 인데 리포가 정본으로 지정한 grep 명령은 **3699** 를 낸다. `a5a3c0f9`
가 넣은 `@MainActor func test` 9건이 앵커 패턴 사각지대에 빠졌기 때문이다. 그래서
"11연속 정적=실측 일치" 주장과 **푸시 직전 사전계산 워크플로가 지금 깨져 있다**.
`README.md:151` 은 아직 3,693 이다(선행 감사에서도 지적).

셈법을 고치든 주석을 고치든 하나로 맞춰야 한다.

## 3. 작업 트리 — 이제 깨끗하다

직전 인계가 "커밋 안 됨" 으로 남긴 P1 부산물 2파일은 `70d1c0ec` 로 착지했다.
`playbackProperties` 는 general.properties 의 playback* 6키에서 value 문자열만 걷고,
빈 값·비문자열·부재는 딕셔너리에 넣지 않는다("부재 = run" 판정을 소비자 한 자리에 모으기 위해서).

**읽는 코드가 아직 없다** — 프로덕션에도 테스트에도. §2-② 가 그 배선이다.

## 4. 이번 라운드가 닫은 P2 미결 (RE 측정)

| 기존 상태 | 판명 |
| --- | --- |
| FFT mixed-radix 재판정 | **해소** — FFTS 정적 링크 확정. 2^n 이면 radix 커널 직행, 아니면 Bluestein(chirp-z) |
| mapsequence 산식 | **해소** — around(opid 13) 속도 혼합 대수식 완정복, 축별 결합 순서까지. Waple `MapSequenceBetweenSolver` 와 일치 |
| TEX CreateTexture2D 포찍 | **해소** — TEX→GPU 포맷 매핑표 정적 복원 |
| `transformnoise` 커널 입력 위상 [미해결] | **해소** — 좌표=(t·scale, 파티클 난수, 0), 레인별 XOR 솔트 0x0B3924AD/0x493A8E83, octaves>12 면 변환 생략 |

### 정정이 필요해진 것

- **`remapinitialvalue` operation 부재 기본** — 직전 인계 §3.2 의 연장선에서, 문서가 주장한
  `"multiply"` 가 아니다. 실제 주입 리터럴은 `"layertime"` 이고, `RemapOperation.swift:137-138`
  의 "int 0/1" 근거 VA 4건(`0x1401bfc8c`/`bfd76`/`bfe64`/`bff52`)은 **명령어 중간 바이트**였다.
- **씬 루트 `version` 게이트(`c2d8ecc1`)** — WE 실물 씬 리더에 대응 분기가 **없다**.
  "코퍼스 재현" 이 아니라 **의도적 이탈**로 재분류해야 한다. 되돌릴지 남길지는 사람 결정.
- **"subsystems 보고의 조작 증거 철회"** — 그 철회 자체가 오류였다. RapidJSON·FFTS·
  `D:\dev\we\windows\src` 경로가 **UTF-16LE 로 실존**한다(ASCII-only 검색 실수). 짝 저장소의
  `analysis/reports/subsystems-identified.md` 머리말이 지금 거짓을 선언하고 있다.

## 5. 만료 확인된 기존 주장 (백로그 정리 대상)

- AUDIT "모든 성공/실패가 NSLog 로만, GUI 비가시" → `notify()` 가 배너·설정미러·트레이·툴팁
  4싱크를 갖는다. (NSAlert·UserNotifications 0건은 여전하나 그건 BACKLOG:311 의 명시적 보류)
- AUDIT/BACKLOG "하드코딩 한국어 40+, 현지화 인프라 전무" → **272키 양방향 차집합 0**,
  테스트 3건이 규약을 강제 중. 실측은 7배 과소 집계였다.
- BACKLOG "설정 창 동영상 미러링" → 그 미러가 `d5d69216`(2026-07-19)에서 삭제돼 결함이 소멸.
- BACKLOG "볼륨/배속 = 전체 재장착" → F820 으로 VideoRenderer 한정 해소. **단 씬 오디오는
  여전히 리마운트 필요**(`SceneAudioPlayer` 가 mount 시 1회 주입).
- 동봉 WEAssets 2,940파일 → sha256 전수 재계산, 누락·미등록·불일치 **0**. WE 설치본과 바이트 동일.

## 6. 남은 작업 원장

### P1 (다음 라운드 본체)
1. **§2-① 캡처 핀** — 클릭 게이트 → 핀 소유권 → 툼스톤 정정.
2. **§2-② 재생정책 배선** — 매니페스트 의존 추가 → `PresetResolver` 드롭 수정 → 순수 게이트는
   AppLogic, 옵저버는 앱 타깃(frontmost=NSWorkspace · onbattery=IOKit · audio=MediaPoller 재사용 ·
   fullscreen=CGWindowList 휴리스틱). VRAM 축은 측정기 없어 미배선 명시.
   **키 부재=run 무회귀** 가 최상위 계약. per-renderer 정밀화는 2단계 스테이징 허용.
3. **§2-⑤ 수치 셈법 통일** — 그 다음에 하한 래칫 관례를 복구.
4. **JSON 파서 파일별 대응** — assert 문자열 귀속 재판정부터. `json_value.cpp:1076` 은 jsoncpp
   소스명인데, 이번 라운드에서 **jsoncpp + RapidJSON 병용**이 확인됐으므로 그 전제로 다시.
   문자열 검색은 듀얼 인코딩(UTF-16LE 함정 — §4 의 오검출이 그 사례다).
5. remap `outputrange*` 소비 배선 시 §4 정정 반영 + `RemapOperation` 부재 기본 재판정.

### P2 이후
- RE: `FUN_140261950` MDL 정독+live dump · `scenescript64.dll` Ghidra(현재 strings+import 만 기록됨)
- 맥 실기: 골든 재베이스라인 → mapsequence 배선 · UI 육안 · strict-concurrency 32건(V 레인이
  재측정 확인: 고유 진단 32곳, WapleCore 기여 0) → Swift 6 v6
- 사람 결정: 동봉 WE 에셋 재배포 권리 · 폰트 라이선스 · §4 의 version 게이트 존치 여부
- 장기: application 유형 지원 · winrtutil IPC · dxgi/ETW · BSim · 코드 정리 잔여

## 7. 다음 세션 체크리스트

- [ ] `git pull` — main 이 `bd1dd6ad` 이상인지 확인. 작업 트리는 깨끗한 상태로 넘긴다
- [ ] `docs/swarm-audit-2026-08-26.md` §3 을 먼저 읽는다 — 좌표가 다 거기 있다
- [ ] 스웜 발견 중 **high 이하는 검증 강도가 균일하지 않다**(게이트웨이 503 으로 검증자 다수
      사망). 고치기 전에 개별 재확인할 것
- [ ] 수정 라운드가 되면: 전수 테스트 → 증가분만큼 `ci.yml` 하한 래칫(단 §2-⑤ 셈법 먼저) →
      분리 커밋 → 푸시
- [ ] 이 문서는 다음 라운드 마감 시 갱신/교체

## 8. 인프라 기록 — 게이트웨이 503

이번 라운드 후반에 추론 게이트웨이가 `no upstream API key is currently available`(HTTP 503)
로 광범위하게 실패했다. 27 워크플로우 중 다수의 P2 검증자·P3 종합자가 이걸로 죽었고,
미완 25레인의 원인도 전부 이것이다 — **작업 실패가 아니다**.

재시도 로직(에이전트 사망 시 최대 2회)은 정상 동작했으나 장애가 지속되면 소진된다.
워크플로우는 `resumeFromRunId` 로 재개하면 완료 에이전트를 캐시에서 회수하므로, 같은 장애를
만나면 중단 → 장애 해소 후 재개가 옳다. 재개 매니페스트 관례는
`.claude/projects/<proj>/<session>/workflows/RESUME.md` 에 남겼다(세션 로컬).
