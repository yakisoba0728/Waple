# 인계 — 2026-08-19 전수 리뷰 세션

> 상태: `claude/codebase-review-2h2kg3`, **CI 초록**(debug·release·spec 세 잡),
> 테스트 **2,299 / 실패 0 / 스킵 47**(스윕 수정 반영 후 재측정, CI run `32243310751`). 골든 `baseline-6f0bcf0` 는 그대로 두었다.
>
> 직전 인계 문서(`handoff-2026-08-17.md`)는 "코드로 밀 수 있는 항목은 소진됐다" 로
> 닫혀 있었다. 전수 리뷰를 돌리자 **아니었다** — 아래가 그 결과다.
>
> **남은 둘은 코드 문제가 아니라 소유자가 결정할 문제다**(§남은 것).

---

## 이 세션이 한 일

에이전트 9개로 두 리포(`Waple`, `Waple-wallpaper-source`)를 전수 리뷰하고,
나온 결함을 고쳤다. 컨테이너에 Swift 툴체인이 없어 **CI 가 컴파일러 역할**을 했다 —
그래서 커밋마다 CI 왕복이 붙어 있고, 실패한 시도까지 커밋 메시지에 남아 있다.

### 결함 수정 (46건)

가장 큰 셋만 적는다. 나머지는 커밋 메시지에 있다.

| 결함 | 증상 |
| --- | --- |
| `PropertyAnimation.firedMarkers` Float 누적 | **6.5일 가동 후 렌더 스레드 무한 루프**. `t += period` 가 `ulp(t) > period` 에서 no-op 이 되고 탈출 조건이 `t <= curF` 뿐이라 갇힌다. 30fps·1프레임 루프 기준 2^24 프레임 |
| 반사/굴절 할당자 뒤바뀜 + `fbSampler` 의 `mip_filter` 누락 | 러프니스 블러 사망. `Mesh3DShaders.swift:737` 에 "왜 안 되지" 로 남아 있던 그 건이다. 원인이 **둘**이라 하나만 고치면 여전히 안 나온다 |
| Steam API 키가 쿼리 파라미터 + `URLSession.shared` | `URLCache.shared` 가 URL 전체를 키로 디스크에 쓴다 → `?key=<시크릿>` 이 평문으로 남는다. README·UI 의 "Keychain 에만 저장" 과 어긋났다 |

동시성 작업 중 별도로 실제 경합 4건을 더 찾아 고쳤다(`LibraryViewModel.importVideoFile`,
스틸 캡처의 시점 혼재, `NowPlayingProvider.fetchArtwork` 타임아웃 경로,
`BaseAssetsSettings.loggedAutoDetectedPaths`).

### 검증 체계 — 이 세션의 실질

시작 시점에 **픽셀 회귀를 자동으로 잡는 장치가 0개**였다. 지금은 CI 가 지킨다.

- **합성 씬 픽셀 골든**(`SyntheticPixelGoldenTests` + `spec/golden/synthetic/`).
  코퍼스가 필요 없어 CI 에서 돈다. 실측 `meanAbsDiff=0.0` — 완전 결정적이다.
- **스킵 트립와이어**. `XCTSkip("no Metal")` 아래 561개가 GPU 없는 환경에서 전부
  스킵돼도 CI 는 초록을 찍었다. 이제 스킵 상한 100 · 실행 하한 2,299.
- **구성 매트릭스**. release 를 CI 에서 한 번도 세운 적이 없었다 —
  세워보니 `@testable import` 가 구조적으로 못 서고 있었다(`-enable-testing` 필요).
- **Linux spec 잡**. `validate.py` 도 spec 테스트도 한 번도 안 돌았다. 매니페스트
  무결성·EOL 속성 검사를 여기서 한다.
- **블렌드 32종 커버리지**. 픽셀까지 구동되던 것이 3종뿐이었다.
- **공용 바이너리 리더 경계 테스트**. `BinaryReading.swift` 는 테스트 참조가 0건이었다.

### 정합성

- 동봉 WE 에셋 **302개가 CRLF→LF 로 손상**된 채 커밋돼 있었고, `manifest.json` 이
  그 손상된 바이트로 기준선을 떠서 **자체 검증만 영원히 통과**하는 상태였다. 복원했다.
- `NOTICE` 가 같은 파일 안에서 에셋을 "동봉한다(§0) / 안 한다(§3)" 를 둘 다 말했다.

---

## 🛑 함정 — 이 문서의 값어치는 여기다

직전 인계 문서도 같은 말을 한다. 아래는 이번 세션에서 **실제로 밟은** 것들이다.

### 1. 격리 애너테이션이 `@objc` 콜백을 조용히 끊는다

`-strict-concurrency=complete` 를 **경고로** 켰을 뿐인데 웹 내비게이션 보안 게이트가
죽었다. SDK 가 완료 핸들러에 붙인 동시성 애너테이션 때문에 우리 시그니처가
optional 요구사항과 어긋나 **witness 자격 → 암묵적 `@objc` → 셀렉터**를 차례로 잃고,
`respondsToSelector:` 가 NO 를 반환해 WebKit 이 기본값 `.allow` 로 갔다.

컴파일 오류 없음. 경고 한 줄. **코드를 읽어서는 절대 못 찾는다** —
악성 내비게이션을 실제로 시도하는 테스트만 잡는다.

해법은 **async 델리게이트 구현**이다. 앞선 두 우회는 실패했고 그 기록이
`WebRenderer.swift` 의 해당 메서드 주석에 있다. **완료 핸들러 형태로 되돌리지 마라.**

같은 위험에 노출된 것: 프레임워크가 셀렉터로 콜백하는 모든 타입.
`required` 요구사항은 불일치가 컴파일 오류로 드러나므로 안전하고,
**`optional` 만 조용히 실패한다**.

### 2. 확장자·디렉터리를 열거하면 반드시 하나를 놓친다

`.gitattributes` 가 WEAssets 텍스트를 확장자별로 `-text` 지정했는데
`*.json` 이 `foo.tex-json` 에 매치되지 않았고(`json` 앞이 `-`), `*.js` 는 목록에 없었다.
302개가 손상됐다.

이번에 골든 이미지 규칙을 추가할 때 **같은 함정 앞**이었다 —
`spec/golden/snapshot/**` 이 아니라 `spec/golden/**` 으로 덮었다.

### 3. "검사하는 척하는 검사" 가 이 리포의 지배적 결함 유형이다

버그 자체보다 이게 더 많았다.

- `mip_filter` 테스트가 이름이 다른 샘플러(`fbSampler`)를 못 봤다 — 결함이 살아 있는
  동안 계속 초록이었다
- `XCTAssertTrue(true)` 한 줄 (`.mdl` 픽스처가 8바이트 매직뿐이라 대상 경로에 닿지도 않음)
- 골든 오라클이 `manifest.json` 을 **자기 자신과** 대조
- `SnapshotTests` 가 비교 로직을 인라인 재구현해 **자기 산수를 단언** — 프로덕션
  로직을 지워도 통과
- `/tmp/waple_gt/luma_baseline.json` 은 없으면 만들고 통과 → 새 머신에서 영원히 실패 불가

**그래서 이번에 추가한 모든 검사에 음성 대조를 붙였다.** 빨강/파랑 프레임이 통과하면
실패시키고, "opacity 무시" 단언에는 opacity 에 반응해야 하는 대조군을 두고,
기준선 부재는 스킵이 아니라 실패로 만들었다. 검사를 추가할 때 **그 검사가 무엇을
못 잡는지**를 먼저 적어라.

### 4. `continue-on-error` 는 잡 결론을 속인다

release 레인의 테스트 실패를 "release 는 통과" 로 잘못 읽었다. 잡 `conclusion` 은
`success` 인데 로그에는 `2 failures` 가 있었다. **잡 결론이 아니라 로그를 봐라.**

### 5. 에이전트 보고를 그대로 믿지 마라

문서 결함 9건 중 **4건은 지적이 틀렸다** — 이력 로그의 과거 항목을 현재 주장으로
읽거나, 다른 대상(`waple-baselines/` vs `spec/golden/snapshot/`)을 혼동했다.
`baseline-7075b74` 는 6곳에 있었는데 **실제 결함은 1곳뿐**이고 나머지는 역사였다.
일괄 치환했으면 이력을 위조했을 것이다.

반대로 에이전트가 **내 진단을 반증한** 경우도 있다(§1 의 "적합성이 통째로 깨졌다" 는
틀렸고, 완료 핸들러를 가진 메서드 하나만 떨어져 나간 것이었다).

---

## 2차 — 6단계 스윕 (같은 날 오후)

위 작업이 끝난 뒤 "더 할 게 있나" 를 확인하려고 에이전트 27개를 6단계로 굴렸다.
**있었다.**

| 단계 | 구성 | 산출 |
| --- | --- | --- |
| P1 | Haiku ×8 | 초벌 28건 |
| P2 | Sonnet ×8 | 신규 17건 제기 |
| P3 | Opus ×4 | 심층 37건 |
| P4 | Sonnet ×4 | 카테고리별 재판정 → CONFIRMED 78 |
| P5 | Opus ×2 | **적대적 반증** → 고유 56건 |
| P6 | Opus ×1 | 종합 → 최종 **45건** |

P5 가 핵심이다. 기본값을 REFUTED 로 두고 5단계(인용 실물 대조 → 이미 수정됨 → 도달 가능성
→ 상위 가드 → 귀결 추적)로 공격하니 **high 가 9건에서 2건으로 줄었다**. 줄어든 것들이
중요하다 — `clip()` 미번역은 도달 경로가 0건이었고(Waple 볼류메트릭은 자체 MSL),
`paths-ignore` 는 "무력화" 가 아니라 "지연" 이었고, `SteamCmdDownloader` 경로 미검증은
우리가 띄운 로컬 프로세스의 자기 목적지였다. **셋 다 내가 사용자에게 결함으로 보고한 뒤
반증됐다.**

### 고친 것

| 커밋 | 내용 |
| --- | --- |
| `8d5d1ba` | 신뢰 경계 밖 정수 좁힘 **13곳** → `safeInt` 정본 하나로 라우팅 + CI 증식 감시 |
| `5df4c36` | ASCII-only 검색이 만든 거짓 부재 정정 (FFTS) |
| `5aee54c` | 중복 키 트랩 2 · localStorage 상한 · **재임포트 평점 소실** |
| `851c213` | 조용히 통과하던 게이트 4종을 실제 게이트로 |
| `cb46339` | 삭제 예정 리포가 유일 근거인 것들을 정본으로 이관 |
| `1cd5438` | 위생 12건 — 전부 "형제는 고쳤는데 여기만" |
| `f1406b2` | `WapleCompat` 분리 — 1,799줄이 처음 테스트 가능 |

### 지배적 실패 방식 셋 — 뿌리는 하나다

**국소적으로 확인한 것을 전역 명제로 승격시킨다.**

1. **★ 수정의 전파 누락 (17건)** — 결함을 발견한 자리에서만 고치고 형제를 쓸지 않는다.
   표본: `SceneRendererFrameEncoder:1758` 이 자기를 막으려고 만들어진 `safeFloatToInt` 의
   **8줄 위**에서 맨 `Int(v)` 를 썼다. 가드가 넷이나 있는데 12곳이 아무것도 안 거쳤다.
   → `scripts/spec/check_int_narrowing.py` 의 **R4 인구조사**가 이 클래스를 기계적으로 막는다.
   실제로 그 검사가 스윕 6단계가 전부 놓친 13번째 자리(`WallpaperProperties:126`)를 찾았다.

2. **★★ ASCII-only 바이너리 검색의 거짓 부재가 정본에 긍정 명제로 박힌다 (9건)** —
   wallpaper64.exe 의 문자열은 **UTF-16LE** 다. ASCII grep 은 0건을 낸다.
   가장 심한 것: 실존하는 경로 21개를 "fabricated" 로 단정했고, "RapidJSON 아님" 의 근거로
   **RapidJSON 자신의 `GetParseError_En()` 표**를 인용했다. `WE-ENGINE-ANALYSIS-2026-07-27.md:69`
   가 이 함정을 **이미 경고했는데** 나중 문서가 맞는 문서를 뒤집었다.
   → `spec/engine/linked-libraries.json`(전수 스캔, 하드코딩 아님)이 근거를 고정한다.
   **부재를 주장하려면 어떤 인코딩으로 어떤 명령을 돌렸는지 함께 적을 것.**

3. **★★★ 도달성 비약 — "자산이 존재한다" → "그 코드 경로가 실행된다" (2건)** —
   둘 다 이 스윕의 **Phase 4 산출물 안에서** 적발됐다.
   → 도달성 주장에는 그 자산을 읽는 Swift 호출부 `file:line` 을 요구할 것.

### 리포 삭제 전에 확인할 것

`Waple-wallpaper-source` 는 삭제 예정이다. 이관은 끝났다:

- `spec/we-install-tree.json` — `ui/`(1,548) · `locale/`(75) 전수 sha256, `projects/`(999)
  트리 다이제스트. **이게 확정 402건 중 유일하게 지문조차 없던 부류였다.**
- `spec/engine/linked-libraries.json` — 링크된 라이브러리 20개 경로 + 어서션 11개.
- `ShaderPreprocessor.swift:33,49` 의 오프셋 규약 역전 정정 — **주입본이 사라지면 검증 불가라
  지금만 가능했다**(원본 `0x48be68` vs 주입본 `0x48bf38`, 정확히 +0xD0).

부수 확증: 설치본 `assets/` 2,940개와 리포 동봉 `WEAssets` 2,940개가 **해시 불일치 0**.

### 아직 남은 것

- `HDRBloomPass.swift:167` 첫 블러 스트라이드 — `strengthScale` 골든 캘리브 상수가 물려
  있어 macOS 골든 재검증이 필요하다. 폴백 전용 경로다.
- `SceneRenderSettingsTests` 의 병렬 실패 — UserDefaults 전역 상태. `SceneCompositeConventionTests`
  쪽 원인(고정 `/tmp` 경로)은 고쳤지만 이쪽은 별개다. `AGENTS.md` 에 확인 명령을 적어 뒀다.
- ~~`.md` 전용 커밋이 CI 를 건너뛰어 spec 근거검사가 지연된다~~ → **해소**(`0635cd8`) —
  Linux 검사를 `spec.yml` 로 떼어내 `paths-ignore` 없이 항상 돌린다. 문서 전용 커밋 `ab122e4`
  에서 `spec.yml` 만 돌고 `ci.yml`(macOS)은 건너뛰는 것을 실측 확인했다.

### 부재 주장 감사 — 음성 결과가 자산이다

스윕 부록이 남긴 최대 공백("나머지 7종 바이너리의 UTF-16LE 부재 검증")을 닫았다.
`scripts/spec/measure_absence_claims.py` → `spec/engine/absence-audit.json`.

설치본 **42종 전부**를 ASCII·UTF-16LE 두 인코딩으로 훑어 정본의 부재 주장 10건을 재판정했다.
**반증 0건.** `formats/tex-deep.json` 의 `binaryTokenCounts` 111개 데이터 포인트도 그대로
재현됐다(불일치 0).

왜 오염되지 않았나가 핵심이다 — **문자열 종류에 따라 인코딩이 다르다**:

  · MSVC 가 넣는 어서션·`__FILE__` 소스 경로 → **UTF-16LE** (`D:\dev\we\…`, `!XMScalarNearEqual(…)`)
  · 저작 스키마 키(.tex-json 키, 셰이더 유니폼 이름) → **ASCII** (런타임이 텍스트에서 파싱한다)

분석 리포의 산문 보고서가 걸린 것은 앞쪽이고, `spec/` 정본이 다루는 것은 대부분 뒤쪽이다.
"전부 UTF-16" 도 "전부 ASCII" 도 아니라는 것 — 이걸 모르면 양쪽으로 다 틀린다.

이 감사에서 배운 것 하나 더: **부재 주장은 범위를 명시해야 판정된다.** 첫 판이 `halfmip` 을
거짓 반증했는데, 주장은 "런타임에 없다" 인데 컴파일러(`resourcecompiler64.exe`)에서 찾았기
때문이다. 지금은 각 주장이 `absentIn`(runtime / we / all / 파일 목록)을 갖는다.

---

## 남은 것

코드로 밀 수 있는 것은 소진했다. 남은 둘은 **결정**이 필요하다.

### A. `LICENSE` 예외 조항 — 소유자 결정

`LICENSE` 는 예외 없는 순수 MIT 인데 WE 에셋 75.8MB 를 동봉해 배포한다.
문면상 작성자가 라이선스할 수 없는 것을 서브라이선스하는 형태다.

이번 세션은 **사실관계만 바로잡았다**(`NOTICE`·README 의 거짓 진술).
조항을 쓰거나 동봉 방침을 바꾸는 것은 하지 않았다. 선택지는 셋으로 보인다 —
예외 조항 추가 / 동봉 중단(현재는 `package-app.sh` 가 에셋 없으면 릴리스를 **실패시킨다**) / 현상 유지.

### B. Swift 6 `.v6` 전환 — 설계 변경 필요

진단 265건 → 70건(74%)까지 줄였다. 남은 70 중 약 45건이 `AppDelegate` 의 **F486
오프메인 마운트 경로**다. 애너테이션으로 없애면 실측으로 수초 스톨을 고친 최적화가
되돌아간다.

진짜 해결은 캡처 경로를 `RendererFactory` 에서 분리하거나 뷰 없는 오프스크린 타깃으로
옮기는 것인데, 성능 실측을 다시 떠야 한다. 현재는 `-strict-concurrency=complete` 가
**경고로** 켜져 있고 CI 가 파일별 건수를 잡 요약에 렌더한다 — 그 숫자가 0 이 되면
모드를 올릴 수 있다.

렌더러에 `@MainActor` 를 붙이는 것이 ~50건의 정답이지만 **테스트 타깃과 한 커밋이어야
한다**. `SceneRenderer` 만 85파일 420곳이 nonisolated XCTest 메서드에서 참조한다.

---

## 재현·검증

```bash
# CI 가 하는 것과 같은 것
swift build -c debug && swift build --build-tests -c debug && swift test --skip-build

# release 는 테스트 빌드에 플래그가 필요하다(§CI 매트릭스 주석 참조)
swift build --build-tests -c release -Xswiftc -enable-testing

# 픽셀 골든 기준선 다시 뜨기 — 눈으로 확인하기 전에 커밋하지 마라
WAPLE_GOLDEN_BOOTSTRAP=1 swift test --filter SyntheticPixelGolden
# 또는 GitHub Actions 에서 CI 워크플로를 golden_bootstrap 입력을 켜고 수동 실행
```

기준선 넷의 기대 픽셀 값은 `spec/golden/synthetic/README.md` 에 있다.
