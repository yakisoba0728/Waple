# 인계 — 2026-08-17 중단 지점

세션을 중간에 멈췄다. **진행분은 전부 원격 브랜치에 있고 유실 없다.** 이 문서만 읽으면
어느 머신에서든 이어받을 수 있다.

```bash
git fetch origin
git checkout -b unit-e   origin/wip/unit-e-menubar-lightmode   # 진행분 9커밋
git checkout -b shape-quad origin/wip/shape-quad               # 진행분 2커밋
```

`main` 은 `a4ea6c0` 이고 **깨끗하다**(CI 초록, 테스트 2,262 / 실패 0). 두 브랜치는
main 에 **아직 안 올렸다** — 아래 완료 조건을 채우고 올려야 한다.

---

## 🛑 먼저 알아야 할 것 — 골든이 일부러 멈춰 있다

`golden-gate.sh` 를 돌리면 **FAIL 37 / PASS 133** 이 나온다. **회귀가 아니다.**
`spec/golden/snapshot/README.md` 상단에 전문이 있다. 요약:

`mul` 인자 순서 전치를 고쳤고(수학적 확정 — `squareToQuad` 코너 항등 오차 0),
그것이 가리고 있던 상류 결함이 드러나면서 일부 씬이 **육안으로 나빠졌다.**
파괴 렌더를 기준선에 구우면 `9a50467` 에서 고친 실수를 반복하는 것이라 보류 중이다.

**게이트가 빨갛다는 이유로 재베이스라인하지 마라.** 아래 A 가 닫혀야 뜬다.

---

## A. `wip/shape-quad` — 골든의 관문 (1순위)

### 상태
| 커밋 | 내용 |
| --- | --- |
| `d92f99b` | 이펙트 캐리어 quad 의 풀스크린 승격을 걷어낸다 — `SceneDocument.swift` + 테스트 2 |
| `2ca485f` | **미완성** — spec 항목과 측정 스크립트 608줄 |

⚠️ **`d92f99b` 의 제목이 "WE 기본 크기 확정" 을 단언하는데 그 등급이 아직 안 선다.**
아래가 닫히기 전에는 제목·등급을 낮춰야 한다. "확정" 이라고 적힌 채 두는 것이 가장 나쁘다.

### 이미 확정된 것 (뒤집을 근거 없으면 신뢰)
- `"quad"` 문자열이 WE 바이너리 **5종 어디에도 없다**(ASCII·UTF-16 0건).
  shape 분기는 JSON **타입 태그**(=4)만 보고 값을 비교하지 않는다 → shape 값은 미해석.
- shape 렌더러블(0x460B, vtable `0x140491D10`)의 vfunc `+0x40` 이
  `(float)(int32)[ctx+0x88]` 을 `this+0x2F0` 에 **movsd**(float 2개)로 쓴다.
- `ctx+0x84 = (int)ortho.width`, `ctx+0x88 = (int)ortho.height`(공유 필드 `+0x11c` 로 교차 확인).

### 남은 것
1. **`this+0x2F0/0x2F4` 의 소비 지점** — "그 필드가 크기다" 를 세우는 열쇠이자 등급을
   추정에서 올리는 유일한 길. 이미지 레이어는 별개 클래스(0x960)라 대조 불가.
2. **교란 ①** `3521337568` 쿼드 부모(id **2065**) scale 미확인 — 부모 변환이 개입하면
   기본 크기와 무관하게 결과가 갈린다.
3. **교란 ②** 비-풀스크린에서 우리 솔리드 레이어 이펙트 체인의 UV/RT 규약 미검증 —
   직전 산출물에 **WE 엔 없는 하드 엣지**가 있었다(WE 는 rayfeather 페이드).
4. 4씬 육안 대조: `3404976219` · `3521337568` · `3558034522` · `3460973721`.
   **`3299228616` 무회귀 확인 필수**(이미 해결된 씬이다).

### 함정 (실제로 걸렸던 것)
- 측정 스크립트는 `WE_BIN` 만으로는 죽는다(`형제 바이너리를 하나도 못 찾았다`).
  형제 5종이 한 디렉터리에 있으니 **`WE_ROOT` 로 줘라**:
  ```bash
  WE_ROOT=~/Downloads/wallpaper_source/binaries \
  WE_WORKSHOP=~/Downloads/wallpaper_dev/backgrounds \
  python3 scripts/spec/measure_shape_quad.py
  ```
  단 그 디렉터리는 설치본 레이아웃이 아니다(`assets/` 없음). 다른 생성기의 `WE_ROOT`
  관례를 깨지 말고 폴백을 더할 것.
- **PE 스큐**: 보관본 `wallpaper64.exe`(5,360,320B)는 e_lfanew 0x110 이고 섹션 테이블
  `PointerToRawData` 가 갱신 안 돼 **208B 어긋난다.** 헤더대로 읽으면 `.pdata` 가 전부 0.
  `measure_texture_filtering.py` 의 `PE` 리더가 보정 코드를 갖고 있다. 깨끗한 설치본은 5,360,112B.
- RenderDoc 은 **1개뿐**이다(`caps/` 없음), 섹션 압축이라 desc 바이트 스캔은 이미 실패했다.

### 완료 조건
**A/B 가 혼재하면 착지시키지 마라.** 앞 단위가 그래서 되돌렸고 그게 옳았다.
못 닫았으면 커밋을 조사 기록으로 남기고 제목을 그렇게 고칠 것.

---

## B. `wip/unit-e-menubar-lightmode` — UI 개편 마지막 단위

### 진행분 9커밋 (전부 커밋 완료)
```
451b3bb en.lproj 의 단위별 마커 구역을 걷고 한 블록으로 합친다 — 중복 2건 제거
7044c26 사이드바 폴더 행에 우클릭 메뉴와 접근성 액션을 붙인다
fd64314 설정 창을 세로로 리사이즈할 수 있게 한다
bd034c1 LibraryViewModel.onError 를 onNotify 로 바꾼다
1a05d68 notify() 호출부를 생산 지점에서 현지화한다
21479d2 AppLogic 의 표시 문자열을 생산 지점에서 현지화한다
dc7dbe6 메뉴바 현지화 결함 3건을 닫고 오라클 사각지대를 함께 막는다
3a31802 콘텐츠 우물을 underPageBackgroundColor 에서 걷어낸다 — 라이트에서 회색 매트였다
db45ffe 창 두 개의 darkAqua 강제를 걷어낸다 — 시스템 외관을 따라가게
```

### 남은 것 — **최종 재판정이 통째로 남았다**
브리프가 요구한 완료 조건은 **5표면 × 라이트/다크 × 한국어/영어 캡처 + 육안 판정**이다.
`darkAqua` 를 걷어낸 것이 이 앱에서 **라이트 모드가 처음 켜지는 것**이라, 판정 없이
main 에 올리면 안 된다. `3a31802` 가 이미 라이트에서 회색 매트 하나를 잡았다 —
같은 종류가 더 있을 수 있다.

```
WAPLE_SMOKE=1                              # 메인창
WAPLE_SMOKE=1 WAPLE_SMOKE_TAB=discover|workshop
WAPLE_SMOKE=1 WAPLE_SMOKE_DISPLAYS=1
WAPLE_SMOKE_SETTINGS=1
WAPLE_SMOKE_ONBOARDING=1
```
배선은 `Shell/SmokeLaunch.swift` 의 순수 함수 하나에 모여 있다(테스트 11개가 지킨다).

### 캡처 함정 — 오늘 실제로 사고가 났던 것들
- ⚠️ **`scripts/window-id.swift` 에 `--pid <PID>` 를 반드시 써라.** 이름 매칭 탓에 한 단위의
  캡처에 **다른 단위의 UI 가 들어간 사고**가 있었다. PID 없이 다른 프로세스 창이 둘 이상이면
  스크립트가 멈춘다(exit 3) — 정상 동작이다.
- ⚠️ **영어는 패키징 앱에서만 나온다.** `.lproj` 가 빌드 산출물에 없어 `swift run` 은 항상
  한국어다. `scripts/package-app.sh` + `-AppleLanguages '(en)'`.
- ⚠️ **이 머신은 Accessibility 미허용이라 합성 입력(`CGEvent`·`osascript`)이 조용히
  전달되지 않는다.** 오류가 안 나서 **거짓 결론**이 된다 — 한 단위가 실제로 속았다.
  클릭·스크롤이 필요한 판정은 "못 했다" 고 적을 것.
- 격리: `CFFIXED_USER_HOME` 으로 가짜 홈(`HOME` 만으로는 안 된다 — Foundation 은 이것만
  따른다), `library.json` 의 `selectedId=null`. 띄웠으면 **반드시 종료**.
  실제 데스크탑 배경을 바꾸지 말 것. 설정 창의 "지금 바탕화면으로 한 번 굽기" 를 **누르지 마라.**
- 시스템 외관을 바꿨으면 **되돌릴 것**(`defaults write -g AppleInterfaceStyle Dark`).

---

## 합류 절차 (두 브랜치 공통)

1. `git cherry-pick` 으로 main 에 올린다. ⚠️ `git cherry` 는 충돌을 손으로 해소하면
   patch-id 가 달라져 "미적용" 으로 잘못 보고한다 — **파일 내용으로 확인할 것.**
2. `swift build --build-tests` → **순차** `swift test`(`--parallel` 은 이 저장소에서 거짓 실패).
3. **테스트 기준값을 재측정해서 한 번에 갱신**한다(`AGENTS.md:59-62`).
   각 단위가 자기 브랜치에서 올리면 안 된다 — 오늘 병렬 단위 둘이 각자 `2,180+3` 을 계산해
   **둘 다 2,183 이라 적는** 사고가 있었다. 현재 main 기준값은 **2,262**.
4. `python3 scripts/spec/validate.py` 오류 0(죽은 참조 검사 포함).
5. 커밋에 **AI 귀속 금지** — `Co-Authored-By` 트레일러·도구 이름 절대 금지.
   태그·원격 브랜치도 대상이다(오늘 `git log --all` 로 27건이 남아 있던 걸 정리했다).

## 그다음 (두 브랜치가 다 착지한 뒤)

- **골든 재베이스라인** — `scripts/mac-session/rebaseline-golden.sh`. 전 코퍼스를 두 번 뜨고
  커서를 옮겨 자기검증하는 ~30분짜리다. 육안 판정할 사람이 필요하다.
- **오디오 축 (2026-08-18 조사로 실태 정정)** — "씬 BGM 미구현" 은 **무효**다.
  `SceneAudioPlayer`(272줄)가 mp3/wav/flac/ogg 를 전부 재생한다. 실제 원인은
  `VideoSettings.volume` 기본 0 + 음량 UI 가 `.video` 로 좁혀진 것이었고 **고쳤다**(`08f5a31`).
  남은 것: **보조 모니터 스펙트럼**(`SceneRenderer.swift:1557` 이 재생용 게이트
  `isPrimaryScreenWindow` 를 스펙트럼 구독에도 재사용 — `WebRenderer` 는 같은 게이트가
  0건이고 `SharedAudioCaptureCore` 가 이미 다중 구독을 지원한다. 술어 분리 한 줄 수준,
  도달 133/170씬) · **스펙트럼 규약 G·선형/로그 미확정**(정적 RE 로는 비용 대비 낮다 —
  RE 보고서가 "FFT 라이브러리 참조 0건" 으로 이미 막혔다. 윈도우 실물에 알려진 신호를
  먹여 읽는 동적 측정이 더 싸다).
- **미착수 감사분** — translucent depthwrite(정본은 "translucent 면 저작값 무관하게 뎁스 쓰기
  해제" 인데 우리는 저작값을 따른다 — `3470948192` 성단 누락의 유력 기전) ·
  씬 BGM 영구 음소거 157씬 · 보조 모니터 오디오 반응 · trail/rope 지오메트리 ·
  파티클 회전 x/y 폐기 33씬
