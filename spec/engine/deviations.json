{
 "weVersion": "2.8.42",
 "generatedBy": "손 작성 — WE 원문(WEAssets/shaders/common_pbr.h, common_blending.h)과 Waple 소스 직접 대조 + macOS A/B 캡처 실측",
 "note": "'의도적 이탈 3건' 으로 시작했으나 대조 결과 하나(D2)는 이탈이 아니었다. 남은 둘(D1·D3)은 A/B 로 화면 영향 0 을 실측하고 **유지**로 확정했다.",
 "entries": [
  {
   "id": "deviation.D1.ggxDenominatorFloor",
   "value": {
    "isDeviation": true,
    "what": "GGX 분모에 바닥값 1e-4 를 씌운다",
    "we": "float denominator = (NH * NH * (rSqr2 - 1.0) + 1.0); return rSqr2 / (M_PI * denominator * denominator);",
    "waple": "float denominator = max(rawDenominator, 1e-4); return r4 / (pi * denominator * denominator);",
    "where": [
     "Sources/WapleRender/Mesh3DShaders.swift `Distribution_GGX` 안의 `float denominator = max(rawDenominator, 1e-4);` (라이브)",
     "Sources/WapleCore/ScenePBRLighting.swift `ggxDenominatorFloor` 상수 선언과 그 사용부 (데드코드 미러 — Sources 참조 0건)"
    ],
    "whereWasLineNumbered": "[정정 2026-09-01] 종전 이 두 자리는 `Mesh3DShaders.swift:171` · `ScenePBRLighting.swift:15` 였고 **둘 다 드리프트해 있었다** — :171 은 빈 줄, :15 는 `genericimage4.frag` 를 언급하는 문서 주석이었다(실측 당시 실제 자리는 :191 · :38/:46). 줄 번호를 다시 박지 않는다: `grep -n 'max(rawDenominator' Sources/WapleRender/Mesh3DShaders.swift` · `grep -n 'ggxDenominatorFloor' Sources/WapleCore/ScenePBRLighting.swift`",
    "divergesWhen": "roughness=0 이고 N·H=1 일 때. 그 지점에서 WE 는 0/0 = NaN, Waple 은 0",
    "abResult": {
     "targets": [
      "3470948192",
      "3589454154",
      "3662790108"
     ],
     "pixelChange": "0 — 최대 채널차 0, FNV 해시 170/170 일치, 썸네일 PNG 바이트 170/170 동일",
     "verifiedLive": "대상 3종 전부 라이트 보유(2/2/1)로 GGX 경로가 실제로 실행됐다",
     "note": "코퍼스 t=6.0 프레임에서 화면이 안 바뀐다는 뜻이지, 제거해도 안전하다는 뜻은 아니다. 제거하면 코퍼스에 없는 입력(roughness=0, N·H=1)에서 NaN 경로가 되살아난다."
    },
    "waplesBehaviorIsStrictlyBetter": "그 지점에서 WE 는 NaN 을 내고 Waple 은 0 을 낸다. NaN 은 픽셀을 오염시킨다"
   },
   "status": "확정",
   "evidence": [
    {
     "kind": "shader",
     "ref": "Sources/WapleRender/Resources/WEAssets/shaders/common_pbr.h:18-25"
    },
    {
     "kind": "file",
     "ref": "Sources/WapleRender/Mesh3DShaders.swift",
     "note": "`Distribution_GGX` 함수 — 종전 `:165-173` 은 point 라이트 감쇠(`pow(falloff…)`) 주석을 가리켜 무관했다"
    },
    {
     "kind": "file",
     "ref": "macOS A/B 캡처 2026-08-01 — A(release,무패치) vs B(D1·D3 제거) 완전 동일"
    }
   ]
  },
  {
   "id": "deviation.D2.geometryNlFloor",
   "value": {
    "isDeviation": false,
    "verdict": "이탈이 아니다 — WE 원문과 동일하다",
    "we": "Schlick_GGX(max(dot(N, V), 0.001), roughness) * Schlick_GGX(max(dot(N, L), 0.001), roughness)",
    "waple": "Schlick_GGX(max(dot(N, V), 0.001), roughness) * Schlick_GGX(max(dot(N, L), 0.001), roughness)",
    "identical": true,
    "alsoIdentical": {
     "FresnelSchlick": "WE 와 Waple 둘 다 pow(max(1.0 - lightTheta, 0.001), 5.0)"
    },
    "howTheErrorHappened": "감사가 '0.001 하한 = Waple 이 넣은 안전장치' 로 분류했으나 WE 원문을 대조하지 않았다. 0.001 은 WE 자신의 값이다.",
    "abMeasurementWasInvalid": "A/B 패치가 ScenePBRLighting.swift(데드코드)만 건드리고 라이브 본체 Mesh3DShaders.swift:182-183 은 손대지 않아 애초에 측정되지 않았다. 그러나 원문 대조로 이탈이 아님이 확정돼 재측정이 불필요해졌다."
   },
   "status": "확정",
   "evidence": [
    {
     "kind": "shader",
     "ref": "Sources/WapleRender/Resources/WEAssets/shaders/common_pbr.h:34-37 (GeoSmith), :4-7 (FresnelSchlick)"
    },
    {
     "kind": "file",
     "ref": "Sources/WapleRender/Mesh3DShaders.swift:181-188"
    }
   ]
  },
  {
   "id": "deviation.D3.blendGuardRange",
   "value": {
    "isDeviation": true,
    "what": "블렌드 가드가 정확 비교가 아니라 범위 비교다",
    "epsilonIsHarmless": {
     "claim": "max(s, 1e-5) / max(1-s, 1e-5) 는 무해하다",
     "why": "엡실론이 발동하는 구간은 이미 min(...,1) 또는 max(...,0) 으로 클램프되는 영역이라 결과가 같다"
    },
    "realDeviation": {
     "we": "(blend == 1.0) ? blend : min(base/(1.0-blend), 1.0)",
     "waple": "select(min(b/max(1.0-s,1e-5), 1.0), 1.0, s >= 1.0)",
     "difference": "가드 조건이 == 1.0(정확) vs >= 1.0(범위). s in [0,1] 에서는 동일",
     "divergesWhen": "s > 1(HDR 슈퍼브라이트) 또는 s < 0(음수 틴트)",
     "concreteExample": "s=2.0 일 때 WE 는 min(base/(1-2), 1) = -base 로 음수를 낸다. Waple 은 1.0 클램프"
    },
    "affectedModes": [
     "3 ColorBurn",
     "8 ColorDodge",
     "14 VividLight",
     "21 Reflect",
     "22 Glow"
    ],
    "where": [
     "Sources/WapleRender/BlendMSL.swift — `we_colorburn` · `we_colordodge` · `we_reflect` (그리고 이 셋을 합성하는 `we_vividlight` · 모드 22 의 인자 뒤집기)",
     "Sources/WapleCore/BuiltinShaderIncludes.swift — `BlendColorBurnEx` · `BlendColorDodgeEx` · `BlendReflectEx` (GLSL 미러)"
    ],
    "whereWasLineNumbered": "[정정 2026-09-01] 종전 이 두 자리는 `BlendMSL.swift:9,10,16` · `BuiltinShaderIncludes.swift:29,32,52` 였는데 **BlendMSL 쪽은 파일 머리의 설명 주석**을 가리켰다(:9 는 `ApplyBlending` 이 32개 `#if` 를 갖는다는 서술). 실제 구현은 `we_*` 인라인 함수들이다. 줄 번호를 다시 박지 않는다: `grep -n 'we_colorburn\\|we_colordodge\\|we_reflect' Sources/WapleRender/BlendMSL.swift`",
    "abResult": {
     "pixelChange": "0",
     "sampleThinness": "패치된 모드를 쓰는 씬이 8종/18레이어인데 그중 HDR 은 3287715210 하나뿐이다. D3 는 [0,1] 밖에서만 갈리므로 실질 표본이 1종이다 — 근거가 얇다"
    }
   },
   "status": "확정",
   "evidence": [
    {
     "kind": "shader",
     "ref": "Sources/WapleRender/Resources/WEAssets/shaders/common_blending.h:114,115,119"
    },
    {
     "kind": "file",
     "ref": "Sources/WapleRender/BlendMSL.swift",
     "note": "`we_colorburn`/`we_colordodge`/`we_reflect` 와 `applyBlending` 의 모드 디스패치 — 종전 `:8-16` 은 파일 머리 설명 주석이었다"
    },
    {
     "kind": "file",
     "ref": "macOS A/B 캡처 2026-08-01"
    }
   ]
  },
  {
   "id": "deviation.finding.scenePBRMathIsDead",
   "value": {
    "what": "ScenePBRMath(Sources/WapleCore/ScenePBRLighting.swift)는 데드코드다",
    "sourcesReferences": 0,
    "testReferences": [
     "Tests/WapleCoreTests/SceneForwardLightingTests.swift",
     "Tests/WapleRenderTests/TubeLightCSMandMipTests.swift"
    ],
    "consequence": "라이브 PBR 은 전부 Mesh3DShaders.swift 의 MSL 이다. CPU 미러를 고쳐도 화면은 안 바뀐다. 이 사실을 몰라서 A/B 패치가 D2 를 데드코드에만 적용했다.",
    "risk": "두 구현이 갈라져도 테스트가 CPU 쪽만 보므로 드리프트를 못 잡는다"
   },
   "status": "확정",
   "evidence": [
    {
     "kind": "file",
     "ref": "grep -rn ScenePBRMath Sources/ -> 정의 파일 외 0건"
    },
    {
     "kind": "file",
     "ref": "grep -rln ScenePBRMath Tests/ -> 2파일"
    }
   ]
  },
  {
   "id": "deviation.abMethod.lessons",
   "value": {
    "releaseVsDebug": {
     "captureSeconds": {
      "debug": 1692.5,
      "release": 159.5
     },
     "speedup": "9.6배",
     "warning": "debug 와 release 는 픽셀이 다르다. 대조군 26종이 바뀐 것처럼 보였는데 A(debug) vs A'(release,무패치) 대조로 전부 빌드 효과임이 드러났다. A/B 는 반드시 같은 빌드 구성끼리 비교해야 한다.",
     "determinism": "release 에서는 3363252053 도 결정이 됐다(170/170). debug 에서만 비결정이었다"
    },
    "relMetricFlaw": {
     "formula": "rel = mean / max(lumaA * 255, 1.0)",
     "problem": "lumaA*255 < 1.0 인 씬은 분모가 1.0 으로 클램프돼 '상대' 가 절대값과 같아진다",
     "affected": "3662790108 (lumaA 0.0034 -> 0.87)",
     "note": "대비 정규화를 노렸는데 정작 가장 어두운 씬에서 작동하지 않았다"
    },
    "targetOvercount": "종전 도달 산정이 objects[].model 만 봤다. PBR 루프는 라이트가 있어야 도는데 3509243656 은 모델 8개·라이트 0이라 GGX 를 아예 안 탄다. 라이트 조건을 추가해 정정"
   },
   "status": "확정",
   "evidence": [
    {
     "kind": "file",
     "ref": "macOS A/B 세션 2026-08-01 실측"
    },
    {
     "kind": "file",
     "ref": "scripts/ab-deviations/03-ab-diff.py:69"
    }
   ]
  },
  {
   "id": "deviation.decision",
   "value": {
    "decided": true,
    "decidedOn": "2026-08-01",
    "basis": "WE 원문 대조 + macOS A/B 캡처(release) 실측",
    "D1": {
     "verdict": "유지",
     "why": "갈리는 지점이 roughness=0 · N·H=1 하나뿐이고 거기서 WE 는 0/0 = NaN 을, Waple 은 0 을 낸다. NaN 은 픽셀을 오염시키므로 Waple 쪽이 엄밀히 낫다. A/B 실측 화면 영향 0.",
     "limits": "A/B 가 답한 것은 '코퍼스 t=6.0 에서 화면이 안 바뀐다' 이지 '제거해도 안전하다' 가 아니다. 반대 방향의 근거는 아니다."
    },
    "D2": {
     "verdict": "이탈 아님 — 목록에서 삭제",
     "why": "WE common_pbr.h 원문과 바이트 단위로 동일하다. 0.001 은 WE 자신의 값이다."
    },
    "D3": {
     "verdict": "유지",
     "why": "A/B 화면 영향 0. 다만 [0,1] 밖 입력을 실제로 만드는 표본이 HDR 1종(3287715210)뿐이라 근거가 얇다 — 제거할 이유가 없다는 쪽이 제거할 이유가 있다는 쪽보다 강하다.",
     "revisitTrigger": "블렌드 모드를 쓰는 HDR 씬이 늘거나, 슈퍼브라이트 입력에서 육안 이상이 보고되면 재검토한다."
    },
    "doNotRediscover": "이 셋은 조사 완료 항목이다. 소스 주석이 이 정본을 가리키므로 '안전장치처럼 보이는 이탈' 로 다시 적출하지 마라."
   },
   "status": "확정",
   "evidence": [
    {
     "kind": "file",
     "ref": "spec/engine/deviations.json deviation.D1/D2/D3"
    },
    {
     "kind": "file",
     "ref": "macOS A/B 캡처 2026-08-01 — A(release) vs B 완전 동일"
    }
   ]
  }
 ]
}
