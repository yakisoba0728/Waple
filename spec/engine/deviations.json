{
 "weVersion": "2.8.42",
 "generatedBy": "손 작성 — WE 원문(WEAssets/shaders/common_blending.h)과 Waple 소스 직접 대조. 재생성 스크립트 없음(대조 결과 기록)",
 "note": "Waple 이 의도적으로 넣은 이탈 3건. 셋 다 [0,1] 입력에서 WE 와 등가고 경계 밖에서만 갈린다. 결정 전 A/B 캡처 대상.",
 "entries": [
  {
   "id": "deviation.D1.ggxDenominatorFloor",
   "value": {
    "what": "GGX 분모에 바닥값 1e-4 를 씌운다",
    "waple": "denominator = max(nh*nh*(r4-1)+1, 1e-4); return r4/(pi*denom*denom)",
    "we": "바닥값 없음 — return rSqr2 / (M_PI * denominator * denominator)",
    "where": [
     "Sources/WapleCore/ScenePBRLighting.swift:7,15",
     "Sources/WapleRender/Mesh3DShaders.swift:171"
    ],
    "divergesWhen": "roughness=0 이고 N·H=1 일 때 WE 는 0/0. 그 외에는 등가",
    "wapleRationale": "소스 주석에 [safety deviation] 로 명시됨 — 0/0 방지",
    "reach": "objects[].model 보유 8씬, 그중 HDR 4씬"
   },
   "status": "확정",
   "evidence": [
    {"kind": "file", "ref": "Sources/WapleCore/ScenePBRLighting.swift:6-17"},
    {"kind": "shader", "ref": "WEAssets/shaders/common_pbr*.h"},
    {"kind": "corpus", "ref": "spec/engine/deviation-reach.json deviation.D1D2.ggxAndNlFloor"}
   ]
  },
  {
   "id": "deviation.D2.geometryNlFloor",
   "value": {
    "what": "Smith geometry 항의 N·V, N·L 에 하한 0.001 을 씌운다",
    "waple": "nv = max(dot(N,V), 0.001); nl = max(dot(N,L), 0.001)",
    "we": "확인 필요 — common_pbr*.h 의 해당 항을 직접 대조할 것",
    "where": ["Sources/WapleCore/ScenePBRLighting.swift:27-28"],
    "divergesWhen": "법선이 뒤를 향할 때(내적 음수). WE 가 0 하한이면 결과가 0, Waple 은 0.001 로 미세 기여",
    "reach": "D1 과 동일 경로 — 8씬, HDR 4씬"
   },
   "status": "보고",
   "evidence": [
    {"kind": "file", "ref": "Sources/WapleCore/ScenePBRLighting.swift:25-31"},
    {"kind": "recon", "ref": "적대적 검증 라운드에서 WE 쪽 대조가 완료되지 않았다"}
   ]
  },
  {
   "id": "deviation.D3.blendGuardRange",
   "value": {
    "what": "블렌드 가드가 정확 비교가 아니라 범위 비교다. 종전에 '엡실론 이탈' 로 묶여 있었으나 대조 결과 둘은 별개다",
    "epsilonIsHarmless": {
     "claim": "max(s, 1e-5) / max(1-s, 1e-5) 는 무해하다",
     "why": "엡실론이 발동하는 구간은 이미 min(...,1) 또는 max(...,0) 으로 클램프되는 영역이라 결과가 같다",
     "example": "colorburn s=1e-9: WE 는 1-(1-b)/1e-9 -> 큰 음수 -> max(...,0)=0. Waple 은 1-(1-b)/1e-5 -> 큰 음수 -> 0. 동일"
    },
    "realDeviation": {
     "we": "(blend == 1.0) ? blend : min(base/(1.0-blend), 1.0)",
     "waple": "select(min(b/max(1.0-s,1e-5), 1.0), 1.0, s >= 1.0)",
     "difference": "가드 조건이 == 1.0(정확) vs >= 1.0(범위). s in [0,1] 에서는 동일",
     "divergesWhen": "s > 1(HDR 슈퍼브라이트) 또는 s < 0(음수 틴트)",
     "concreteExample": "s=2.0 일 때 WE 는 min(base/(1-2), 1) = -base 로 **음수**를 낸다. Waple 은 1.0 으로 클램프한다"
    },
    "affectedModes": ["colorburn", "colordodge", "reflect", "vividlight(파생)", "glow(reflect 파생)"],
    "where": [
     "Sources/WapleRender/BlendMSL.swift:9,10,16",
     "Sources/WapleCore/BuiltinShaderIncludes.swift:29,32,52"
    ],
    "wapleRationale": "소스 주석: 'HDR 슈퍼브라이트/음수 틴트 발산 해소'",
    "reach": "colorBlendMode != 0 인 67씬, 그중 HDR 9씬",
    "decisionFraming": "WE 의 발산(음수 출력)을 그대로 재현할 것인가"
   },
   "status": "확정",
   "evidence": [
    {"kind": "shader", "ref": "Sources/WapleRender/Resources/WEAssets/shaders/common_blending.h:114,115,119"},
    {"kind": "file", "ref": "Sources/WapleRender/BlendMSL.swift:8-16"},
    {"kind": "corpus", "ref": "spec/engine/deviation-reach.json deviation.D3.blendEpsilon"}
   ]
  },
  {
   "id": "deviation.decision.status",
   "value": {
    "decided": false,
    "plan": "HDR 씬 A/B 캡처 후 결정한다",
    "sideA": "spec/golden/snapshot/baseline-81098bb (변경 전, 커밋됨)",
    "sideB": "이탈 제거 후 재캡처",
    "targets": {"D1D2": 4, "D3": 9},
    "controlGroup": "비-HDR 3D 씬 4종 — [0,1] 입력이라 A/B 가 동일해야 한다. 바뀌면 수정이 의도 밖 경로를 건드린 것이다",
    "gateWarning": "D1/D2 대상 4종 중 3470948192·3662790108 은 골든 사각지대다(spec/golden/gate-analysis.json). 이 둘은 현행 게이트로 판정 불가 — 직접 픽셀 통계로 재야 한다"
   },
   "status": "확정",
   "evidence": [
    {"kind": "file", "ref": "spec/golden/gate-analysis.json oracle.gate.blindScenes"},
    {"kind": "corpus", "ref": "spec/engine/deviation-reach.json deviation.reach.abPlan"}
   ]
  }
 ]
}
