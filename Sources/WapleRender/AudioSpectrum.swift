import WapleCore

/// 비닝·정규화 유틸은 WapleCore 로 일원화(WapleCore.AudioSpectrum). 여기서는 재수출만 —
/// 모듈 로컬 typealias 라 WapleRender 전 파일에서 별도 import 없이 `AudioSpectrum` 을 그대로 쓴다.
public typealias AudioSpectrum = WapleCore.AudioSpectrum
